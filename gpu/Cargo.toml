[package]
name = "hack-gpu"
version = "0.1.0"
edition = "2024"

[lib]
path = "lib.rs"

[build-dependencies]
naga = { version = "30.0.1", features = ["wgsl-in", "spv-out"] }

[dependencies]
ash = "0.38.0"
rayon = "1.12.0"
