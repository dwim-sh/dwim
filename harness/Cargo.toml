[package]
name = "dwim-harness"
version = "0.1.0"
edition = "2024"

[lib]
path = "lib.rs"

[dependencies]
dwim-models = { version = "0.1.0", path = "../models" }
libc = "0.2.189"

[dev-dependencies]
serde_json = "1.0.151"
